export const ROUTE_PREFIXES = {
  auth: "/auth",
  genres: "/genres",
  health: "/health",
  languages: "/languages",
  lists: "/lists",
  movies: "/movies",
  uploads: "/uploads",
  users: "/users"
} as const;
